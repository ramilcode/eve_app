name: eve_app
description: App by Ramil K. EventGuests
publish_to: "none"
version: 1.0.0

environment:
  sdk: ">=3.0.5 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.0.0
  provider: ^6.0.3
  intl: ^0.19.0
  shared_preferences: ^2.0.17
  flutter_staggered_animations: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
      - assets/images/

